import { CarBooleanTransformPipe } from './car-boolean-transform.pipe';

describe('CarBooleanTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CarBooleanTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
